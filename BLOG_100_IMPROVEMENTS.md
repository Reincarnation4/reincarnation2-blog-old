# Blog 100 项改进清单

本轮以“更松弛、更易读、更像一个真正的个人出版物”为总方向，100 项均已落实到首页、阅读层、数据层或管理员路由。

## A. 信息架构与导航

1. 将首页定位从“演示 CRUD”改为个人出版物首页。
2. 增加明确的品牌副标题与站点使命。
3. 使用固定顶部导航，避免页面滚动后迷路。
4. 提供首页、文章、关于、管理四个主要入口。
5. 增加当前导航项高亮。
6. 将管理员入口从主视觉中降级为轻量入口。
7. 使用文章数量、精选文章和最近更新组成内容索引。
8. 增加“跳到正文”链接，改善键盘与读屏体验。
9. 统一所有外链的打开与返回逻辑。
10. 统一站点的页脚信息与数据连接状态。

## B. 视觉与版式

11. 用暖纸色背景替代压迫感较强的纯色页面。
12. 采用衬线正文与无衬线 UI 的双字体系统。
13. 将内容宽度限制在舒适阅读范围内。
14. 取消挤在一起的三列密集卡片布局。
15. 采用“一篇主文章 + 纵向文章流”的层级结构。
16. 增加夸张但克制的首页标题排版。
17. 用编号、日期和阅读时长建立视觉节奏。
18. 用细线、留白和色块代替厚重边框。
19. 给精选文章使用独立视觉区块。
20. 给文章卡片加入封面比例约束。
21. 给图片增加圆角、遮罩和焦点位置控制。
22. 给按钮增加明确的 hover、focus、active 状态。
23. 统一圆角、阴影、边距和颜色 token。
24. 将错误、成功、空状态使用不同语义色。
25. 加入低强度纸张纹理，避免背景过于平。
26. 增加渐进式出现动画，但限制在 300ms 内。
27. 尊重 prefers-reduced-motion。
28. 避免所有标题使用全大写造成的压迫感。
29. 让移动端标题尺寸独立适配。
30. 让图片在窄屏上保持完整构图。

## C. 内容发现与阅读

31. 首页优先展示精选文章。
32. 最近文章按发布时间倒序。
33. 支持按关键词搜索标题、摘要和正文。
34. 支持按分类过滤文章。
35. 支持按“有图/精选/全部”筛选。
36. 增加搜索结果数量反馈。
37. 增加无结果状态和清空筛选按钮。
38. 支持 URL query 打开指定文章，便于分享。
39. 阅读详情显示真实日期、分类、阅读时长。
40. 详情页显示文章封面。
41. 详情页正文使用更宽松的行高。
42. 详情页正文限制最大行宽。
43. 详情页显示阅读进度条。
44. 增加返回文章列表按钮。
45. 增加上一篇、下一篇导航。
46. 增加复制文章链接按钮。
47. 增加 Web Share API 分享按钮并提供复制降级。
48. 增加文章内容复制反馈。
49. 文章切换不触发整页刷新。
50. 浏览器后退可以关闭详情或恢复列表状态。
51. 支持键盘 Escape 关闭详情。
52. 支持键盘 / 聚焦搜索框。
53. 加入懒加载图片。
54. 加入图片加载失败占位。
55. 为图片补充可访问 alt 文本。
56. 提供阅读模式切换。
57. 提供深色模式切换。
58. 记住用户的主题选择。
59. 记住用户上次的筛选状态。
60. 给外部图片统一使用 referrer policy。

## D. 互动与数据体验

61. 首页保留真实 Supabase 连接状态。
62. 文章读取显示 skeleton 而不是空白。
63. 所有网络错误都显示可理解的中文说明。
64. 刷新数据按钮加入忙碌状态。
65. 新文章发布支持标题自动生成 slug。
66. 新文章发布支持封面 URL。
67. 新文章发布显示写入成功提示。
68. 发布失败保留表单内容，避免丢稿。
69. 留言板支持读取最近留言。
70. 留言提交后立即刷新列表。
71. 留言提交支持忙碌状态。
72. 表单支持 Enter 提交、Shift+Enter 换行。
73. 表单输入增加长度上限提示。
74. 文章列表使用稳定的数据库 id 作为 key。
75. 日期统一使用本地化格式。
76. 文章状态使用可读中文标签。
77. 统计面板数据与 Supabase 实际数据同步。
78. 文章数据加载不再依赖 localStorage。
79. 图片上传直接进入 Supabase Storage。
80. 封面 URL 在保存前可预览。

## E. 管理后台

81. 后台使用独立 admin.html 路由。
82. 后台必须通过 Supabase Auth 登录。
83. 登录后检查 profiles.role 是否为 admin。
84. 后台侧栏显示文章总数。
85. 后台列表显示发布状态和封面状态。
86. 后台列表支持关键词过滤。
87. 后台编辑器支持新建和编辑复用。
88. 后台支持草稿、发布、归档。
89. 后台支持精选状态。
90. 后台支持封面 URL 编辑。
91. 后台支持图片文件上传。
92. 上传文件名自动清洗并加时间戳。
93. 上传成功自动写回公共 Storage URL。
94. 后台提供图片预览。
95. 后台支持删除文章。
96. 删除操作提供二次确认。
97. 后台保存成功后刷新左侧列表。
98. 后台错误反馈不使用 alert 打断编辑。
99. 后台退出登录后清理 Supabase 会话。
100. 后台和首页共享相同的 Supabase 数据源与视觉 token。

## 自检结果

- 数据：旧版 6 篇文章已恢复到 Supabase，历史 3 张图片已迁移到 `blog-media/legacy/`。
- 读写：首页读取、发布、留言；后台登录、编辑、删除、上传均接入 Supabase。
- 视觉：首页和后台均重新组织为留白优先、内容优先的布局。
- 可访问性：键盘焦点、语义按钮、图片 alt、减少动画模式已覆盖。
- 发布：本文件与网页文件一起提交到 GitHub Pages。

> 说明：本清单中的 100 项不是未来待办，而是本次迭代的验收项；后续修改应先更新清单再改页面。
